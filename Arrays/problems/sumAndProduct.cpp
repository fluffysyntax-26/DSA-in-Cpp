#include <iostream>

using namespace std; 

int ArraySum(int arr[], int size){ 
    int sum = 0; 
    for(int i=0; i<size; i++){ 
        sum += arr[i]; 
    }

    return sum; 
}

int ArrayProduct(int arr[], int size){ 
    int product = 0; 
    for(int i=0; i<size; i++){ 
        product += arr[i]; 
    }

    return product; 
}

int main(){ 
    
    int arr[] = 
    
}
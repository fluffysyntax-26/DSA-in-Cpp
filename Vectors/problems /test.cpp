#include <iostream> 

using namespace std; 

void twoSum(int *nums, int target, int size) {
    
}

int main(){ 
        
    int arr[] = {2,7,11,15}; 
    int targetSum = 9; 

    int size = (sizeof(arr)/sizeof(arr[0])); 
    cout << size << endl; 
    
    twoSum(arr, targetSum, size); 

}